import 'package:flutter/material.dart';

class CricketButton extends StatelessWidget {
  const CricketButton({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
