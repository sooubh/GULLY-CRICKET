import 'package:flutter/material.dart';

class BallTimeline extends StatelessWidget {
  const BallTimeline({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
