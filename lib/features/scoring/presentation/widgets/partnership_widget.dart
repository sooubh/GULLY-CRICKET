import 'package:flutter/material.dart';

class PartnershipWidget extends StatelessWidget {
  const PartnershipWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
